Class session on Tuesday, February 3rd, 2026.
Present: Dave Hulme and Connor White
Absent: Jacob Brown

NOTES:

* Dave will continue to update the project board and make assignments. Check your assignments and let him know if anything needs to be reassigned.
* Data dictionary is in good shape, but it will need to be modified to reflect the name of the final output "joined files" (see below) instead of df_a and df_b
    * As we add additional files OR add additional columns from current files, the data directory will be updated. To save time, we will likely not remove columns from either (unless the files become unwieldy)
* Python code to be added to the src directory as .py files:
    * Processing: imports interim files, exports as .csv (see below) [Connor]
    * Joining: imports processed files, exports as joined .csv (see below) [Connor]
 * Notebook needs to be added to the notebooks directory incorporating the scripts used to make the current "results" graphs. It will need to be modified to import from the joined files (see below) [Dave]


As per the professor, the structure for Team Grapefruit is approved to be:

* raw: original downloaded files
* interim: copies of the originals, modified in Excel (reduced rows / columns)
* processed:
    * interim files, imported into Python and exported out as .csv ("processed files")
    * processed files, imported into Python, joined together and exported out as .csv ("joined files")
 
