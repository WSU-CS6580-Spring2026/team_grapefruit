During the class on Thursday, January 29th, a portion of the class was dedicated to a work session.
Present were Dave Hulme (first five minutes only), Connor White, and Jacob Brown. 
During the brief period that Dave was there, it was suggested by Jacob that the team export a csv after some processing by Python. Moving forward, the folders will be:
* Raw - original downloaded files
* Interim - downloaded files, but simplified and streamlined in Excel (by filtering / deleting rows and deleting columns)
* Processed - interim files, with additional processing done in Python

Jacob was given the assignment to work on setting up the processing code (but not push it before the current project is graded, just for safety) and Connor was going to switch up the code and the server to have the files in the correct location. 
Due to the structure outlined, the server should only need to have the interim files (uploaded) and processed files (output from Python).
